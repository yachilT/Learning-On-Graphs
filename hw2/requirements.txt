# LoG HW2 — Spatio-Temporal Forecasting on Graphs
# Notebook imports: math, random (stdlib) + the packages below.
# PyTorch Geometric is NOT required (GCN layer is implemented from scratch).
#
# Environment: conda env "LoG", Python 3.14.4
# GPU: NVIDIA GeForce RTX 4090 (sm_89, 24 GB), CUDA-enabled torch build.
#
# To reproduce the CUDA torch build, install it from the PyTorch index:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu130
# (The notebook also runs on CPU if no GPU is present — torch auto-falls back.)
numpy==2.4.4
networkx==3.6.1
matplotlib==3.10.9
torch==2.11.0+cu130
tqdm==4.67.3
ipython==9.13.0
pillow==12.2.0
